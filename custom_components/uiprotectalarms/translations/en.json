{
    "config": {
      "step": {
        "user": {
          "title": "Enter Unifi Protect Host IP, Username and Password",
          "data": {
            "host": "Host IP (IPV4)",
            "username": "Username",
            "password": "Password"
          }
        }
      },
      "error": {
        "invalid_auth": "Authentication failed."
      },
      "abort": {
        "single_instance_allowed": "Only one instance is allowed."
      }
    },
    "options": {
      "step": {
        "init": {
          "title": "Uiprotectalarms Options",
          "data": {
            "auto_reconnect": "Automatically reconnect if the websocket drops."
          }
        }
      }
    }
  }